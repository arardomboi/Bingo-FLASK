# Bingo FLASK — real-time bingo board

This small Flask app serves a 5x5 bingo board. When any user clicks a square, the change is broadcast to all connected users using WebSockets (Flask-SocketIO).

wizard shit it works btw (though ts is just prompt-engineering maxxing)